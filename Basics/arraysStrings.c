/* Arrays: Declare and allocate space for array A */
#include <stdio.h>

int A[10];
for (int i = 0; i < 10; i++) {
	A[i] = 0;
}

/* Strings: arrays of char terminated by \0 */
char[] name = "carlitos";
name[4] = "5";